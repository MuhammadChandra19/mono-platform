// Jest setup file for @packages/pkg
// This runs before each test file

// Mock environment variables if needed
process.env.NODE_ENV = "test";

// Add custom matchers or global test utilities here if needed
